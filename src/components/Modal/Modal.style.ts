import styled from "styled-components";

export const ModalConstainer = styled.dialog``;

export const CloseModal = styled.button``;

export const Previous = styled.button``;

export const Next = styled.button``;